# 为 OpenWiki 贡献代码

感谢你的贡献！我们对 PR 贡献的标准是 **一个 PR = 一个变更**。
这能让我们保持快速审查和干净的提交历史。

## 范围：一个 PR = 一个变更

Pull request 应该有良好的范围控制，每个 PR 只做一件事。

修复你正在做的变更中涉及的 bug 是可以的，但如果你在过程中发现需要修复 _不相关的_ 东西，请为它单独开一个 PR。

### "范围紧凑"的含义

✅ **好的示例：**"将 Fireworks 添加到模型提供商列表"——包括提供商配置、其模型选项和对应的文档行。

❌ **范围过大：**"添加一个新提供商，重构凭证引导流程，并修复 README 中的一个拼写错误"——三个不相关的变更。你应该将它们拆分成三个 PR。

## 在提交 PR 之前

在本地运行以下命令，以免被 CI 意外拦截：

```sh
pnpm run format
pnpm run lint
pnpm test
```

`format` 和 `lint` 与每个 PR 上运行的检查一致，`test` 运行 Vitest 测试套件。

## PR 要求

- **清晰的标题**——用一句话描述这一个变更，以 [Conventional Commits](https://www.conventionalcommits.org/) 类型为前缀，例如 `feat:`、`fix:` 或 `chore:`（如 `feat: add Fireworks to the model provider list`）。
- **做什么和为什么**——简要说明 PR 做了什么以及原因。
- **如何测试**——描述验证你的变更有效且不会破坏现有行为的测试（单元测试或端到端测试）。如果你添加或更新了测试，请在此说明。
- **关联 issue**——对于任何非平凡的变更，请关联一个 issue，以便变更具有上下文。

## 给 AI 代理的注意事项

如果你是在本仓库中提交 PR 的代理，这些规则是强制性的。将你的变更严格限制在单一关注点上。**如果你即将做的变更会违反本文档中的任何内容，请停止并向人类反馈，而不是继续执行。**

## 什么情况下会被关闭

打包了多个不相关变更的 PR 可能会被关闭，并要求拆分为独立的、范围紧凑的 PR。
