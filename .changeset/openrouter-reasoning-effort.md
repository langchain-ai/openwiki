---
"openwiki": minor
---

feat: support OPENWIKI_REASONING_EFFORT on the openrouter provider, forwarding the effort through OpenRouter's normalized `reasoning` parameter instead of rejecting every model
